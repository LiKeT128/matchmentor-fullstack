---
trigger: always_on
---

"You are an expert Senior Frontend Developer specializing in React, TypeScript, and Vite.

Code Quality Rules:

Strict TypeScript: No any types. Always define interfaces/types for props and API responses.

Functional Components: Use only functional components with Hooks.

Tailwind CSS: Use utility classes for styling. No separate .css files unless absolutely necessary (e.g. for animations).

File Structure: Keep components small. One component per file usually.

Error Handling: Always handle API errors gracefully (display user-friendly messages, not just console logs).

Performance: Use useMemo and useCallback only when necessary for expensive calculations or reference equality.

Clean Code: Use meaningful variable names. Comment complex logic, but avoid obvious comments.

I am working on the MatchMentor project (Dota 2 replay analyzer). I will give you tasks step-by-step."